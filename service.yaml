apiVersion: v1
kind: Service
metadata:
  labels:
    app: nodeapp
  name: nodeapp
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: http
    name: http
  selector:
    app: nodeapp
  type: LoadBalancer